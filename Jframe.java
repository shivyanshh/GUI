import java.awt.Color;

import javax.swing.ImageIcon;
import javax.swing.JFrame;
public class Jframe{
    public static void main(String[] args) {
        /* 
        //JFrame = GUI window to add components to
        JFrame frame = new JFrame();

        frame.setVisible(true); //to make frame visible
        frame.setSize(420,420); //to set its size
        frame.setTitle("JFrame testing");//to set title
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        //this closes the program and the jframe when u close the jframe window
        frame.setResizable(false); //prevent window from resizing

        ImageIcon image = new ImageIcon("C:\\Users\\shivy\\OneDrive\\Pictures\\images\\smart-cat.jpg");
        frame.setIconImage(image.getImage()); //to set an logo to your jframe

        //frame.getContentPane().setBackground(Color.RED);
        frame.getContentPane().setBackground(new Color(0x123456));
        */
    }
}